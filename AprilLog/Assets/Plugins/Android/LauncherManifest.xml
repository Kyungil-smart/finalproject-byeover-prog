<?xml version="1.0" encoding="utf-8"?>
<!-- 담당자 : 조규민 -->
<!-- 구현원리 : Android launcher 모듈의 첫 Activity 단계부터 portrait를 고정해 Unity 실행 화면 회전을 방지한다. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="preferExternal">

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/app_icon">

        <!-- 추가: 조규민 - UnityPlayerActivity가 런처로 병합될 때 OS 스플래시 단계부터 portrait를 강제한다. -->
        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@style/UnityThemeSelector"
            android:screenOrientation="portrait"
            android:resizeableActivity="false"
            android:exported="true"
            tools:replace="android:theme,android:screenOrientation,android:resizeableActivity,android:exported">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>

    </application>
</manifest>
